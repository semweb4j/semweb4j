<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<!-- 
		WARNING: Do not invoke mvn release:prepare/perform on this project.
		It just garbles all POMs up but doesn't work.
		Even with Maven 2.0.6-
		
		wth: you might use it for site deployment thought
	-->
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.semweb4j</groupId>
		<artifactId>parent</artifactId>
		<version>4.15.3</version>
	</parent>

	<groupId>org.semweb4j</groupId>
	<artifactId>rdf2go.project</artifactId>
	<version>4.4.6</version>
	<packaging>pom</packaging>

	<name>Semweb4j RDF2go project summary</name>
	<url>http://www.semweb4j.org/rdf2go</url>
	<inceptionYear>2005</inceptionYear>
	<description>
		RDF2go is an implementation-independent Java API with the design
		goals: portability (hence the name), performance, easy of
		implementation.
	</description>

	<issueManagement>
		<system>JIRA</system>
		<url>http://issues.semweb4j.org/browse/RTGO</url>
	</issueManagement>

	<modules>
		<module>../org.semweb4j.rdf2go.api</module>
		<module>../org.semweb4j.rdf2go.impl.base</module>
		<module>../org.semweb4j.rdf2go.impl.base.test</module>
		<module>../org.semweb4j.rdf2go.impl.jena24</module>
		<module>../org.semweb4j.rdf2go.site</module>
<!-- 
		<module>../org.semweb4j.rdf2go.layer.autopersist</module>
 -->
	</modules>

	<!-- to avoid folded site uris -->
	<distributionManagement>
		<site>
			<id>theoretiker-site</id>
			<name>RDFReactor site</name>
			<!--  mvn creates new dirs if necessary -->
			<url>
				scp://theoretiker.org/home/max/public_html/semweb4j.org/site/rdf2go
			</url>
		</site>
	</distributionManagement>

	<!-- to find the parent -->
	<repositories>
		<repository>
			<id>theoretiker-repo</id>
			<name>Semweb4j.org maven repo</name>
			<url>http://semweb4j.org/repo/</url>
			<releases>
				<enabled>true</enabled>
				<updatePolicy>always</updatePolicy>
				<checksumPolicy>fail</checksumPolicy>
			</releases>
			<snapshots>
				<enabled>false</enabled>
			</snapshots>
		</repository>
	</repositories>

	<!-- create a distribution -->
	<build>
		<plugins>
			<!-- builds a zip release that contains all dependency libraries 
				they are copied after compile is executed -->
			<plugin>
				<artifactId>maven-assembly-plugin</artifactId>
				<executions>
					<execution>
						<!-- due to a bug in maven 2.0.x we bind
							this plugin to the integration-test phase
							so it is made sure alle modules are already packaged
							when we assemble
						-->
						<phase>integration-test</phase>
						<goals>
							<goal>attached</goal>
						</goals>
						<configuration>
							<finalName>
								site/sources/${project.artifactId}-${version}
							</finalName>
							<descriptors>
								<descriptor>
									${basedir}/src/assembly/modules.xml
								</descriptor>
							</descriptors>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

</project>